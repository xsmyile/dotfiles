# Local-only by default. Set 0.0.0.0 to listen on all IPv4 interfaces.
SEARXNG_HOST=127.0.0.1
SEARXNG_PORT=8080

# Generate with: openssl rand -hex 32
SEARXNG_SECRET=
SEARXNG_VALKEY_URL=valkey://valkey:6379/0
